# DESIGN.md - Modbus Master Station

## 气质与意象
- 工业控制室中的 SCADA/HMI 操作终端，深夜值班室里唯一亮着的屏幕
- 深色背景上跳动着实时数据，绿色指示灯闪烁，像工厂控制面板一样精确可靠
- 关键词：工业、精密、可靠、数据密集、暗色调

## 视觉策略
- 深色主题为主，模拟工业控制终端的视觉体验
- 数据表格密集排列，信息密度高但层次分明
- 状态指示灯使用圆点 + 颜色编码（绿=正常/连接，红=故障/断开，黄=警告，蓝=操作中）
- 使用等宽字体展示寄存器数据，增强数据可读性

## 配色方案
- 背景：深灰黑 `#0a0e17` → `#111827`（深夜控制室背景）
- 面板：`#1a1f2e` → `#1e293b`（控制面板底色）
- 边框：`#2a3042`（设备面板分隔线）
- 主色调：青绿 `#00d4aa`（信号指示灯，操作高亮）
- 辅助色：琥珀 `#f59e0b`（警告/写入操作）、红色 `#ef4444`（错误/断开）、蓝色 `#3b82f6`（信息/读取操作）
- 文字：主文字 `#e2e8f0`，次要 `#94a3b8`，暗淡 `#64748b`

## 字体排版
- 数据展示：JetBrains Mono / Fira Code（等宽字体，寄存器地址和数值）
- 界面文字：系统默认无衬线字体
- 标题使用大写字母 + 宽字间距，模拟工业铭牌效果

## 动效与交互
- 状态切换使用平滑过渡（200ms ease）
- 数据更新时短暂高亮闪烁（模拟数据刷新指示）
- 按钮操作有明确的按压反馈
- 连接/断开动画模拟设备启动/关闭

## 设计禁忌
- 不要使用明亮的渐变或花哨的装饰
- 不要使用圆角过大的卡片（工业风格偏硬朗）
- 不要使用过于轻量的字体粗细（需要清晰可读）
- 不要添加无关的插画或图标装饰
